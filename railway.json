{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "rm -rf public/build && composer install --no-dev --optimize-autoloader && npm install --include=dev && npm run build"
  }
}
