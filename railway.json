{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "rm -rf node_modules public/build && composer install --no-dev --optimize-autoloader && npm ci --include=dev && npm run build"
  }
}
